# 东方江湖 · 视觉设计系统（v5）

> 本文档是 Jianghu 网站的「单一事实来源（single source of truth）」设计系统。
> 它服务于一次彻底的视觉重构：从「普通 AI 网页」升级为「东方文字修行体验游戏」。

---

## 一、产品定义（最重要）

**本网站不是传统国风网页，而是一款「东方文字修行体验游戏」。**

- 体验由 **文字、留白、节奏、交互反馈** 构成，视觉只是衬托。
- 视觉冲击来自「瞬间的交互反馈」，不是来自一直在动的背景。
- 大量呼吸空间；视觉重点靠 **排版、比例、材质、层级、微 3D、交互**，不靠图片。
- 七张身份大图只存在于「我的侠客 / 个人」页作沉浸式背景，其他页面一律禁用。

## 二、核心视觉方向

`现代高级 UI 骨架 + 东方古风气质 + 微微 3D 空间 + 极少量抽象东方视觉元素 + 有仪式感的交互动画`

## 三、明令禁止的「普通 AI 网页」视觉语言

| 禁止项 | 说明 |
|---|---|
| 高饱和蓝紫渐变 | 任何 `linear/radial-gradient` 出现明显紫蓝 |
| 满屏玻璃卡片 | 玻璃拟态堆叠成卡片墙 |
| 巨大圆角卡片 | 圆角 > 10px 视为「重」 |
| 厚重阴影 | `blur > 40px` 且 `spread` 大的投影 |
| 廉价发光边框 | 霓虹描边 / glow border |
| 3D 旋转按钮 / 卡片翻转 | 任何 rotateY / rotateX 装饰 |
| 过度渐变 / 彩色霓虹 | — |
| 复杂装饰 / 到处图标 | 每个功能都配一个图标 |
| 把每个功能都做成按钮 | 用「可触碰元素」替代按钮 |

## 四、设计 Token

### 颜色（墨黑 / 墨灰 / 雾白 / 低饱和青绿 / 灰蓝 / 温润棕 + 极少暖金）
```
--ink-0:#0a0c0e   最深背景
--ink-1:#0f1316
--ink-2:#151a1e
--ink-3:#1c2228
--fog:#ece9e1      主文字（雾白）
--fog-soft:rgba(236,233,225,.66)  辅助文字
--ink-soft:rgba(220,217,208,.50)
--ink-faint:rgba(220,217,208,.30)
--jade:#6f8f86     低饱和青绿
--blue:#5d7689     灰蓝
--brown:#8a7a64    温润棕
--gold:#c9a86a     暖金（唯一强调色，极少量）
--gold-bright:#e0c389
--gold-soft:rgba(201,168,106,.55)
--gold-faint:rgba(201,168,106,.18)
--hairline:rgba(201,168,106,.20)       边线（金调，极淡）
--hairline-soft:rgba(236,233,225,.09)  边线（雾白，极淡）
--hairline-strong:rgba(201,168,106,.38)
```

### 玻璃 / 阴影 / 模糊（克制）
```
--glass:rgba(18,22,26,.42)      半透玻璃（比 v4 更淡）
--glass-2:rgba(12,16,20,.36)
--shadow-soft:0 8px 24px -16px rgba(0,0,0,.5)
--shadow-deep:0 18px 48px -28px rgba(0,0,0,.55)   ← 比 v4 明显更轻
--blur-glass:10px
```

### 圆角规则（几乎不用圆角，靠角标）
```
--r-node:999px   仅用于极小圆点
--r-soft:6px     极轻圆角
--r-card:4px     卡片近乎方正，角部用「角标」强调而非圆角
```

### 间距系统（8 基数）
```
--sp-1:8px  --sp-2:16px  --sp-3:24px  --sp-4:36px  --sp-5:52px  --sp-6:76px
```

### 排版
```
--fs-display:clamp(3rem,9vw,5.4rem)   标题（Ma Shan Zheng）
--fs-title:clamp(2rem,6vw,3rem)
--fs-h2:1.5rem
--fs-body:1rem
--fs-small:.86rem
--fs-tiny:.74rem
line-height 正文 1.9
```

### 缓动 / 时长（缓入缓出 + 微惯性，无弹跳）
```
--ease:cubic-bezier(.22,.61,.36,1)
--ease-soft:cubic-bezier(.4,.14,.3,1)
--ease-inout:cubic-bezier(.45,.05,.35,1)
--t-fast:180ms  --t-mid:420ms  --t-slow:900ms
```

### 景深层级（微 3D，非真 3D）
```
背景层  z0  bgFar .018 / bgMid .030 / bgCloud .055 / bgMist .075
内容层  z2  文字、修行内容、主要信息
交互层  z60 点击反馈、粒子、光晕、波纹、空间变化
```

### 粒子强度
默认极低（环境音粒子 ≤ 26）；不做粒子雨。

## 五、三层空间系统

1. **背景层**：极淡纸张 / 墨迹 / 山水线稿 / 雾气 / 东方纹理。永远在最远，不参与主视觉。
2. **内容层**：文字、修行内容、主要信息。居中、稳定、可读。
3. **交互层**：点击反馈、粒子、光晕、波纹、空间变化。最前，仅瞬时出现。

所有元素有前后关系，但不做成复杂 3D。

## 六、重新定义「可点击元素」

以后**尽可能不使用传统矩形按钮**。按钮由以下元素构成：
`文字 + 细墨线 + 印记 + 极细边线 + 东方符号 + 局部光影`。

- 默认：安静克制。
- hover（指针设备）：轻微浮起、墨线展开或光影靠近。
- pressed：轻微下沉 / 空间压缩。
- click：极短墨色扩散 / 细波纹 / 光痕 / 粒子聚合 / 印记亮起。
- 页面跳转：极轻空间推进 / 纸页转场。

动画必须短、顺滑、有缓入缓出与轻微惯性；**禁止弹跳、闪烁、无限旋转**。

## 七、按钮 / 交互组件系统

| 组件 | 类名 | 形态 | 状态 |
|---|---|---|---|
| 主要操作 | `.jx-btn` | 文字 + 极细墨线框 + 角标 + 小型印记 | 默认静 / hover 浮起+墨线展开 / pressed 下沉 / click 墨扩散 |
| 文字链接 / 次级 | `.jx-link` | 文字 + 底部细墨线（hover 展开） | 默认 / hover 墨线生长 |
| 分类切换 | `.jx-tab` | 文字 + 移动墨线指示 | 默认 / active 墨线停驻 / hover 微亮 |
| 时间节点 | `.jx-node` | 极小圆点 / 印记 / 线性节点 | 默认暗 / active 墨环+强调 |
| 身份节点 | `.jx-idnode` | 抽象符号 + 名字 + 细线 | 默认 / hover 浮起+符号亮 / active 持久墨线 |
| 返回 | `.jx-back` | 极细符号 + 文字（无框） | 默认 / active 符号亮 |
| 确认 | `.jx-confirm` | 同 `.jx-btn`，确认瞬间印记亮起 | click 印记 gold 亮 .8s |
| 弹窗 | `.jx-modal` | 浮纸 + 角标，无大圆角 | — |
| 页转 | `.view.enter` | 空间推进 + 纸页淡入 | — |

## 八、动效系统

- 统一 `ease`；时长 180–900ms。
- 点击反馈分 **变体**（非单一）：`ink`（墨扩散）、`ripple`（圆波）、`seal`（印记亮）、`line`（细线展开）、`gather`（粒子聚）。
- 背景永远慢（`cloudDrift 46s` 级）；交互永远短（< 720ms）。
- `prefers-reduced-motion` 全量降级。

## 九、七身份抽象视觉语言（无人物、无新图）

仅通过颜色倾向 / 线条运动 / 粒子方向 / 纹理 / 点击反馈区分：

| 身份 | 抽象符号 | 线条运动 | 点击反馈 |
|---|---|---|---|
| 游侠 | 流动风线 | 不规则轨迹横移 | wind：墨线随风掠过 |
| 世家 | 对称玉印 | 稳定对称光泽 | jade：对称金光聚 |
| 书院 | 纸页+墨线 | 文字般线条 | ink：墨线书写 |
| 医者 | 简约叶片+柔光点 | 呼吸光点 | breathe：光点聚散 |
| 琴师 | 水波+弦线+声波 | 同心圆扩散 | ripple：水波荡开 |
| 剑客 | 极细锐利切线 | 短暂光痕横扫 | sharp：光痕一闪 |
| 隐士 | 山形+雾气 | 安静粒子 | mist：淡雾漂动 |

七张已生成身份大图**仅**用于「我的侠客 / 个人」页背景（`#personaBg`，暗角+渐变+微视差）。

## 十、今日修行

原 `seg-art` 简约插图**必须保留**（清晨/上午/下午/傍晚/暮色细线 SVG）。
时间阶段用统一时间轴/东方节点结构，当前节点有明显但克制的视觉状态。
点击节点 → 内容以纸页/墨迹/轻空间移动展开。插图可极轻视差/浮动/亮度变化，但自身不乱动。

## 十一、当前组件审计（需替换）

| 现状（v4） | 问题 | 改造 |
|---|---|---|
| `.btn-enter` / `.home-cultivate` / `.btn-primary` | 金渐变填充，AI 感重 | → `.jx-btn` 无填充，墨线+印记 |
| `.id-token` / `.library-card` / `.stats-panel` / `.identity-card-mini` | 厚玻璃圆角卡片 | → 浮纸/书页 + 角标 + 极细边线 |
| `.library-tab` | 圆角药丸 | → `.jx-tab` 文字+墨线指示 |
| 全局 `border-radius:14–18px` | 圆角偏重 | → ≤ 6px，靠角标 |
| `--shadow-deep` 偏重 | 厚重阴影 | → 减轻 |
| 点击反馈单一（金涟漪） | 无分化 | → 5 种变体 |
| 身份选择（抽象节点） | 已合规 | 精修保留 |
| 今日修行 `seg-art` | 已合规 | 保留 |
| 个人页 `personaBg` | 已合规 | 保留 |

## 十二、素材库结构

```
assets/jianghu/
  ornaments/          东方细线元素（山/云/竹/水/窗棂/书）
  effects/            交互特效元素（墨扩散/水波/细线展开/光痕/印记亮/雾）
  identity/           七身份抽象符号（无人物）
  background-elements/ 背景层素材（纸纹/远山/云带）
```
所有 SVG：合理 viewBox、透明背景、可缩放、可用 `currentColor`、可用 CSS 控制颜色与透明度、路径尽量精简。
